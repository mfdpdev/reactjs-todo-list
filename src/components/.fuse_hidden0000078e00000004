import { useState } from "react";

function Footer(){
  const [modal, setModal] = useState(false);
  return (
    <>
      <div>
        <button onClick={() => {
          setModal(true);
        }} className="w-full bg-red-500 hover:bg-red-600 text-white font-bold py-3 px-3 rounded-xl">
          + Create New
        </button>
      </div>
      <div onClick={() => setModal(false)} className={`w-full bottom-0 rounded-b-none rounded-2xl left-0 ${modal ? 'h-full' : 'h-0 bg-transparent'} transition-all bg-black/25 absolute`} >
        <div className={`w-full rounded-2xl rounded-b-none h-1/2 transition-all bg-white absolute bottom-0 px-4`} >
          <h1 className="my-6 text-center">New Task Todo</h1>
          <hr />
          <div className="h-5/6 gap-4 grid grid-rows-4 grid-cols-2 items-center">
            <div className="col-span-2"></div>
            <div className="col-span-2"></div>
            <div></div>
            <div></div>
            <div>
              <button onClick={() => setModal(false)} className="w-full bg-white hover:bg-red-600 text-red-500 border border-red-500 font-bold py-3 px-3 rounded-xl">
                Close
              </button>
            </div>
            <div>
              <button onClick={() => {}} className="w-full bg-red-500 hover:bg-red-600 text-white font-bold py-3 px-3 rounded-xl">
                Create
              </button>
            </div>
          </div>
        </div>
      </div>
    </>
  )
}

export default Footer;
